---
"todobud": patch
---

`todobud update` waits up to 10 seconds for npm instead of 1, no longer prints the update notice twice, and a failed check keeps the last known release so the automatic notice still appears.
